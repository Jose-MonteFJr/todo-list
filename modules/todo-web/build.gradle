dependencies {
	compileOnly group: "com.liferay.portal", name: "release.portal.api"

	// 1. Conecta o seu Portlet ao seu banco de dados (Service Builder)
	compileOnly project(":modules:todo:todo-api")

	// 2. Traz os métodos para salvar imagens na biblioteca nativa do Liferay
	compileOnly group: "com.liferay", name: "com.liferay.document.library.api"

	cssBuilder group: "com.liferay", name: "com.liferay.css.builder", version: "3.1.4"
}